- Aggregation bedeutet, dass eine Klasse aus mehreren Klassen zusammengesetzt ist
- Die aggregierten Klassen können auch einzeln existieren, jedoch kann die Klasse, die aggregiert, nicht ohne ihre Teile existieren

